---
description: "Analyze reports and generate OpenSpec proposals for skill/prompt improvements"
argument-hint: "[<report-name>]"
---

Load the `opsx-learn` skill and follow its workflow to analyze ${@:-all reports} in `./openspec/reports/` and generate OpenSpec proposals for improvements.
