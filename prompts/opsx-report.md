---
description: "Generate self-reflection on an archived OpenSpec change"
argument-hint: "[<change-name>]"
---

Load the `openspec-report` skill and follow its workflow to generate self-reflection (meditation) on ${@:-the latest archived change}.

**Store handling:** If a store is selected, pass `--store <id>` on all openspec commands (sticky). **Remaining args:** ${@}
